name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      VITE_FIREBASE_API_KEY: AIzaSyCI2XwYL18fGDkuZ1LxHt4Uu2dZdkV4da4
      VITE_FIREBASE_AUTH_DOMAIN: english-progress-hub.firebaseapp.com
      VITE_FIREBASE_PROJECT_ID: english-progress-hub
      VITE_FIREBASE_STORAGE_BUCKET: english-progress-hub.firebasestorage.app
      VITE_FIREBASE_MESSAGING_SENDER_ID: '295122705401'
      VITE_FIREBASE_APP_ID: 1:295122705401:web:f18fd77f3a48447f56bb28
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: Add SPA fallback
        run: cp dist/index.html dist/404.html
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
