export function ProgressBar({value,color='cyan',label}){return <div className="progress-wrap">{label&&<div className="progress-label"><span>{label}</span><b>{value}%</b></div>}<div className="progress"><span className={color} style={{width:`${value}%`}}/></div></div>}
export function Ring({value,size=150,label}){return <div className="ring" style={{'--value':`${value*3.6}deg`,width:size,height:size}}><div><strong>{value}%</strong>{label&&<span>{label}</span>}</div></div>}
export function Sparkline({values,color='#22d3ee'}){const min=Math.min(...values)-3,max=Math.max(...values)+3;const pts=values.map((v,i)=>`${i*100/(values.length-1)},${36-(v-min)*30/(max-min)}`).join(' ');return <svg className="spark" viewBox="0 0 100 40" preserveAspectRatio="none"><polyline points={pts} fill="none" stroke={color} strokeWidth="3" vectorEffect="non-scaling-stroke"/></svg>}
